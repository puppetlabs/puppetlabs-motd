name: "mend"

on:
  pull_request:
    branches:
      - "main"
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:

- name: View PR author
  run: echo "This PR is opened by ${{ github.event.pull_request.user.login }} ."
